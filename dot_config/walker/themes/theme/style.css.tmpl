/* Walker Theme */
/* Generated from theme template */

* {
  all: unset;
  font-family: "JetBrainsMono Nerd Font", monospace;
  font-size: 13px;
}

.box-wrapper {
  background: #301832;
  border: 1px solid #603460;
  border-radius: 12px;
  padding: 16px;
  box-shadow:
    0 19px 38px rgba(0, 0, 0, 0.3),
    0 15px 12px rgba(0, 0, 0, 0.22);
}

/* Search input */
.search-container {
  border-radius: 8px;
}

.input {
  background: #361b38;
  color: #f6f3f1;
  caret-color: #ffffff;
  padding: 10px 14px;
  border-radius: 8px;
}

.input placeholder {
  opacity: 0.5;
}

.input selection {
  background: #49254c;
}

.input:focus {
  background: #3f2042;
}

/* Scrollbar */
scrollbar {
  opacity: 0;
}

/* Results list */
.list {
  color: #f6f3f1;
}

.item-box {
  border-radius: 8px;
  padding: 10px;
}

child:selected .item-box {
  background: rgba(73, 37, 76, 0.80);
}

child:hover .item-box {
  background: rgba(63, 32, 66, 0.50);
}

.item-subtext {
  font-size: 12px;
  color: #e6ddd8;
}

.item-quick-activation {
  background: rgba(54, 27, 56, 0.50);
  border-radius: 6px;
  padding: 8px;
}

/* Icons */
.normal-icons {
  -gtk-icon-size: 16px;
}

.large-icons {
  -gtk-icon-size: 32px;
}

.item-image-text {
  font-size: 28px;
}

/* Preview panel */
.preview-box,
.placeholder {
  color: #f6f3f1;
}

.preview {
  background: #361b38;
  border: 1px solid #603460;
  border-radius: 8px;
  color: #f6f3f1;
}

.preview .large-icons {
  -gtk-icon-size: 64px;
}

/* Keybinds */
.keybinds {
  padding-top: 10px;
  border-top: 1px solid #603460;
  font-size: 12px;
  color: #e6ddd8;
}

.keybind-button {
  opacity: 0.5;
}

.keybind-button:hover {
  opacity: 0.75;
}

.keybind-bind {
  text-transform: lowercase;
  opacity: 0.35;
}

.keybind-label {
  padding: 2px 4px;
  border-radius: 4px;
  border: 1px solid #e6ddd8;
}

/* Calculator */
.calc .item-text {
  font-size: 24px;
}

/* Symbols */
.symbols .item-image {
  font-size: 24px;
}

/* Error state */
.error {
  padding: 10px;
  background: #e28181;
  color: #301832;
  border-radius: 8px;
}

/* Popover */
popover {
  background: #301832;
  border: 1px solid #603460;
  border-radius: 12px;
  padding: 10px;
}

/* Provider-specific */
.providerlist .item-subtext {
  font-size: unset;
  opacity: 0.75;
}

.todo.done .item-text-box {
  opacity: 0.25;
}

.todo.urgent {
  font-size: 24px;
}

.todo.active {
  font-weight: bold;
}

.bluetooth.disconnected {
  opacity: 0.5;
}

:not(.calc).current {
  font-style: italic;
}

.preview-content.archlinuxpkgs {
  font-family: monospace;
}
